{
  "name": "yosnowmow-firebase",
  "version": "1.0.0",
  "description": "Firebase emulator seed, admin scripts, and security rules tests for YoSnowMow",
  "scripts": {
    "seed": "node seed-emulator.js",
    "test:rules": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^10.12.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.test.mjs"],
    "transform": {}
  }
}
